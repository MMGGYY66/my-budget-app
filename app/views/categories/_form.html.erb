<%= form_with(model: category) do |form| %>
  <% if category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
        <% category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name, required:true, class:"form-control" %>
  </div>

  <div class="mb-3">
    <%= form.label :icon, style: "display: block" %>
    <%= form.text_field :icon, required:true, class:"form-control" %>
  </div>

<div class="fixed-bottom w-100 d-flex align-items-center justify-content-center bg-success p-4 border-top border-light shadow-md">     
    <%= form.submit class: "btn btn-outline-light btn-block text-uppercase" %>
  </div>
<% end %>